name: book_club
description: A Flutter application for managing a book club.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=3.0.0 <4.0.0'

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
